void setup() {
  size(500, 500);
  background(255);
  noStroke();
  colorMode(HSB,100);
}

void draw () {
  int i = 30;
  while (i<width-15) {
    int j = 30;
    while (j<height-15) {
      fill(random(100), 100, 100);
      ellipse(i, j, 15, 15 );
      j+=20;
    }
    i+=20;
  }
  noLoop();
}
