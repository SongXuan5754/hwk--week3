void setup() {
  size(800, 800);
  background(255);
  fill(255);
  stroke(0);
  
}

void draw() {
 int i = 30;
  while (i<width-15) {
    int j = 30;
    while (j<height-15) {
     if (random(0,10) < 5){ 
      rect(i, j, 15, 15 );
     }
      j+=20;
    }
    i+=20;
  }
  noLoop();
}
