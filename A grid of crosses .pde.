void setup() {
  size(500, 500);
  background(255);
}

void draw () {
  int x = 20;
  while (x< width-15) {
    int y = 20;
    while (y< height-15){
    line(x, y-4, x, y+4);
    line(x-4, y, x+4, y);
    y+=20;
    }
    x+=20;
  }
 
}
